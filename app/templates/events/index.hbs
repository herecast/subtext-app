<div class='container'>
  <div class='pull-left ContextBar-eventCount'>
    showing {{model.length}} of {{totalEvents}} events
  </div>
</div>

{{#if model}}
  {{grouped-events
    events=model
    category=category
    query=query
    startDate=startDate
    stopDate=stopDate
    location=location
  }}
{{else}}
  <div class='NoResults container text-center'>
    <p>I can't believe it. No one is hosting an event that matches your search.</p>
    <p>You could try tweaking your search for another event or&hellip;</p>
    <p>Seize the opportunity and host your own.</p>
  </div>
{{/if}}
