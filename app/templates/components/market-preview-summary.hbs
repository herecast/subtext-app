<div class='row'>
  <div class='col-sm-3'>
    {{#link-to editLink class='Button btn btn-default pull-left'}}
      {{fa-icon 'arrow-left u-iconLeft'}}
      Edit Listing
    {{/link-to}}
  </div>
  <div class='ContentPreview-summary col-sm-6'>
    <p>
      Preview your market listing page below. If everything looks good to go, click the
      “SAVE &amp; PUBLISH” button to submit your listing.
    </p>
    <p>
      Here’s how you’ve chosen to promote your listing:
    </p>
    <ul class='ContentPreview-propertyList'>
      {{#disabled-checkbox
        checked=model.extendedReachEnabled
        tagName='li'
        class='ContentPreview-summaryProperty'
      }}
        Extend Your Reach
      {{/disabled-checkbox}}
      {{#disabled-checkbox
        checked=model.listsEnabled
        tagName='li'
        class='ContentPreview-summaryProperty'
      }}
        Alert the List
      {{/disabled-checkbox}}
    </ul>
    <div class='ContentPreview-helpText ContentPreview--bottom'>
      Everything below dotted line is your preview
      {{fa-icon 'arrow-down'}}
    </div>
  </div>
  <div class='col-sm-3'>
    {{#async-button action='save' class='Button Button--market btn btn-primary pull-right'}}
      {{#if isSaving}}
        Please Wait&hellip;
      {{else}}
        {{#if media.isTablet}}
          Publish
        {{else}}
          Save &amp; Publish
        {{/if}}
        {{fa-icon 'check u-iconRight'}}
      {{/if}}
    {{/async-button}}
  </div>
</div>
