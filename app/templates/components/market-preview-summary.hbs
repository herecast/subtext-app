<div class='row'>
  <div class='ContentPreview-navigation'>
    <div class='col-sm-3 editLink'>
      {{#link-to editLink class='Button btn btn-default pull-left'}}
        {{fa-icon 'arrow-left u-iconLeft'}}
        Edit Listing
      {{/link-to}}
    </div>
    <div class='col-sm-3 publishLink'>
      {{#async-button action='save' class='Button Button--market btn btn-primary pull-right save-and-publish'}}
        <span {{action 'trackEvent' 'submitContent'}}>
          {{#if isSaving}}
            Please Wait&hellip;
          {{else}}
            {{#if media.isTablet}}
              Publish
            {{else}}
              Save &amp; Publish
            {{/if}}
            {{fa-icon 'check u-iconRight'}}
          {{/if}}
        </span>
      {{/async-button}}
    </div>
  </div>
  <div class='ContentPreview-summary col-sm-6'>
    <div>
      Preview your market listing page below. If everything looks good to go, click the “SAVE &amp; PUBLISH” button to submit your listing.
    </div>
    <div>
      Here’s how you’ve chosen to promote your listing:
    </div>
    <ul class='ContentPreview-propertyList'>
      {{#disabled-checkbox
        checked=model.myTownOnly
        tagName='li'
        class='ContentPreview-summaryProperty'
      }}
        My Town Only
      {{/disabled-checkbox}}
      {{#disabled-checkbox
        checked=model.listsEnabled
        tagName='li'
        class='ContentPreview-summaryProperty'
      }}
        Alert the List
      {{/disabled-checkbox}}
    </ul>
    <div class='ContentPreview-helpText ContentPreview--bottom'>
      Everything below dotted line is your preview
      {{fa-icon 'arrow-down'}}
    </div>
  </div>
</div>
