{{#if isTalk}}
  {{talk-card
    talk=item
    isContentCard=true
    isSimilarContent=isSimilarContent
    sourceContentId=sourceContentId
    class='Card Card--narrow'
  }}

{{else if isNews}}
  {{news-card
    item=item
    isSimilarContent=isSimilarContent
    sourceContentId=sourceContentId
  }}

{{else if isMarketPost}}
  {{market-card
    post=item
    class='Card'
    isSimilarContent=isSimilarContent
    sourceContentId=sourceContentId
  }}

{{else if isEventInstance}}
  {{event-card
    event=item
    class='Card u-flexColumn'
    isSimilarContent=isSimilarContent
    sourceContentId=sourceContentId
  }}
{{/if}}
