{{#if showButton}}
  {{#link-to route class=buttonClass data-test-link='content-create-button'}}
    <span {{action 'trackEvent' 'selectNavControl' linkText}}>
      {{linkText}}
    </span>
  {{/link-to}}
{{/if}}
