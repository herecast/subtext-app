{{#if content}}
  {{yield}}
  <div class="Section">
    <div class="row">
      {{#each contentToDisplay as |content|}}
        <div class="col-sm-6">
          {{small-card
            item=content
            isSimilarContent=true
          }}
        </div>
      {{/each}}
    </div>
  </div>
{{/if}}
