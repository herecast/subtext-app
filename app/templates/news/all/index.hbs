<div class='Container--noGutter'>
  <div class='pull-right ContextBar'>
    <span class='ContextBar-eventCount'>
      Showing {{newsGroups.firstObject.length}} listings.
    </span>

    {{#if showReset}}
      <a href='#' {{action 'resetFilters'}} class='ContextBar-filterReset btn-default btn-xs fa fa-times-circle'>
        Reset search filters
      </a>
    {{/if}}
  </div>
</div>

{{#if newsGroups.firstObject}}
  <div class='Container--noGutter'>
    <div class='hidden-xs'>
      {{#each newsGroups as |news|}}
        {{grouped-news news=news}}
      {{/each}}
    </div>
    <div class='visible-xs'>
      {{ordered-news news=news}}
    </div>
  </div>
{{else}}
  <div class='NoResults container text-center'>
    <p>I can't believe it. No one has posted any news that matches your search.</p>
    <p>You could try tweaking your search or&hellip;</p>
    <p>Seize the opportunity and post your own.</p>
  </div>
{{/if}}
