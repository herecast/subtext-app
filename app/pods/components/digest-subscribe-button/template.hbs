{{#x-button
    onClick=(action 'toggleSubscription')
    style='regular'
    color=(if hasSubscription 'secondary' 'primary')
    size='x-small'
  }}
  {{#if hasSubscription}}
    Unsubscribe
  {{else}}
    Subscribe
  {{/if}}
{{/x-button}}
