{{#modal-wrapper
  close=closeDetailPage
  showHeader=false
  slideInFrom=(if isMobile "left" null)
  slideOutTo=(if isMobile "left" null)
  slideMessage=(if isMobile slideMessage null)
  revealNav=true
  class="Modal--no-padding"
  isDetailPage=true
  willAnimateAway=isMobile
}}

  {{detail-page
    model=model
    closeRoute="feed"
    isPreview=false
    onClose=closeDetailPage
    trackDetailEngagement=trackDetailEngagement
  }}

{{/modal-wrapper}}
