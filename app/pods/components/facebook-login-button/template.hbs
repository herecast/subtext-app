{{#if hasBlock}}
  {{yield this}}
{{else}}
  {{#x-async-button
    promiseAction=(action "loginWithFacebook")
    class="btn btn-lg btn-block btn-social btn-facebook FacebookLoginButton"
  as |promise|}}
    {{fa-icon "facebook"}}
    {{#if promise.isPending}}
      Signing in...
    {{else}}
      Continue with Facebook
    {{/if}}
  {{/x-async-button}}
{{/if}}
