{{#if isMyStuffOnly}}
  {{#if session.isAuthenticated}}
    {{#if isSearchActive}}
      {{no-results-card}}
    {{else}}
      {{no-content-card}}
    {{/if}}
  {{else}}
    {{#if fastboot.isFastBoot}}
      {{x-loader}}
    {{else}}
      {{register-promo-card}}
    {{/if}}
  {{/if}}
{{else}}
  {{no-results-card}}
{{/if}}
