{{#if hasBlock}}
  {{yield}}
{{else}}
  {{textarea
    placeholder=placholder
    class=textareaClass
  }}
{{/if}}

{{#if showImageModal}}
  {{#modal-wrapper close=(action 'cancelImageModal')}}
    {{image-editor
    save=(action 'saveImageModal')
    cancel=(action 'cancelImageModal')
    }}
  {{/modal-wrapper}}
{{/if}}
