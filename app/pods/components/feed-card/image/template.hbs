{{#if hasImage}}
  {{#feed-card/conditional-link-to
    linkUrl=linkUrl
    useLink=linkToDetailIsActive
    model=model
    onContentClick=onContentClick
  }}
    <div class="FeedCard-Image-image" style={{imageStyle}} data-test-card-image></div>
    {{#if hasMultipleImages}}
      <div class="FeedCard-Image-multiple-wrap" data-test-card-multiple-images>
        <div class="FeedCard-Image-multiple-stack">
          <div class="FeedCard-Image-multiple-icon other"></div>
          <div class="FeedCard-Image-multiple-icon icon">
            {{fa-icon "camera-retro"}}
          </div>
        </div>
      </div>
    {{/if}}
  {{/feed-card/conditional-link-to}}
  {{#if hasExcerpt}}
    <div class="FeedCard-Image-excerpt" data-test-card-excerpt>
      {{excerpt}}
      {{#if (and showContinueReading linkToDetailIsActive)}}
        <div class="FeedCard-Image-continue-reading" {{action onContentClick}} role="button" data-test-card-continue-reading>
          {{#link-to-content model}}
            ...Continue Reading
          {{/link-to-content}}
        </div>
      {{/if}}
    </div>
  {{/if}}
{{/if}}
