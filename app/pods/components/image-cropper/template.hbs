{{#form-group}}
  <img class="img-responsive js-Cropper-image" alt="Responsive Image">
  <div class="ImageCropper-buttonContainer">
    <button class="Button Button--icon" {{action "rotateImage" "left"}} title="Rotate image left">{{fa-icon "rotate-left"}}</button>
    <span>Rotate Image</span>
    <button class="Button Button--icon" {{action "rotateImage" "right"}} title="Rotate image right">{{fa-icon "rotate-right"}}</button>

    {{#unless hideCancel}}
      <div class="text-left">
        <button class="ImageCropper-cancelButton" {{action "cancel"}}>Cancel</button>
      </div>
    {{/unless}}
  </div>
{{/form-group}}
