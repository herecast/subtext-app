<a href="tel:+1{{phone}}" class="visible-xs-inline" title="Call">
  {{fa-icon 'phone'}}
  {{#unless hideLabel}}Call{{/unless}}
</a>
<span class="hidden-xs">
  {{fa-icon 'phone'}} {{formatted-phone phone}}
</span>
