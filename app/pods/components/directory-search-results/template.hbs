{{#each results as |result|}}
  <li>
    <div class="listing-info left">
      <div class="name-bar">
        {{#link-to 'directory.search.show' result}}
          <h2>{{result.name}}</h2>
        {{/link-to}}
        <div>
          {{fa-icon 'fa-map-marker'}}
          {{result.fullAddress}}
          ({{round (geo-distance result.coords searchCoords) 1}} mi)
        </div>
      </div>
      <div class="link-bar">
        {{#if result.phone}}
        <div>
          {{#if media.isMobile}}
            <a href="tel:+1{{result.phone}}">
              {{fa-icon 'fa-phone'}} {{formatted-phone result.phone}}
            </a>
          {{else}}
            {{fa-icon 'fa-phone'}} {{formatted-phone result.phone}}
          {{/if}}
        </div>
        {{/if}}
        {{#if result.address}}
          <div><a href="{{result.directionsLink}}" target="_blank">{{fa-icon 'fa-automobile'}} Directions</a></div>
        {{/if}}
        {{#if result.website}}
          <div><a href="{{result.websiteLink}}" target="_blank">{{fa-icon 'fa-laptop'}} Website</a></div>
        {{/if}}
      </div>
    </div>
    <div class="listing-info right">
      {{x-gage
        record=result
        value=result.feedback.recommend
        feedback_num=result.feedback_num
        title=(action 'buildGaugeTitle')
        width=150
        height=100
      }}
    </div>
  </li>
{{/each}}
