{{#if showLoader}}
  {{x-loader}}
{{else}}
  {{news-editor
    news=news
    title=title
    showPreview=preview
    afterPublish="afterPublish"
    refreshModel=(action "refreshModel")
  }}
{{/if}}
{{intercom-button}}
