{{#modal-wrapper class="Modal--no-padding" close=(action 'closeDetailPage') showHeader=false}}
  {{#if (eq ctype 'news')}}
    {{news-detail
    model=model
    closeRoute='index'
    closeLabel='DailyUV'
    isPreview=false
    }}
  {{else if (eq ctype 'event')}}
    {{event-detail
    model=model
    closeRoute='index'
    closeLabel='DailyUV'
    }}
  {{else if (eq ctype 'market')}}
    {{market-detail
    model=model
    closeRoute='index'
    closeLabel='DailyUV'
    }}
  {{else if (eq ctype 'talk')}}
    {{talk-detail
    model=model
    closeRoute='index'
    closeLabel='DailyUV'
    }}
  {{/if}}
{{/modal-wrapper}}
