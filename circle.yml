machine:
  node:
    version: 0.12.0
dependencies:
  pre:
    - npm install -g bower
  override:
    - npm install
    - bower install
deployment:
  staging:
    branch: master
    commands:
      - npm install -g ember-cli divshot-cli
      - ember divshot push staging --token $DIVSHOT_TOKEN
