machine:
  node:
    version: 0.10.28
dependencies:
  pre:
    - npm install -g bower
  override:
    - npm install
    - bower install
deployment:
  staging:
    branch: master
    commands:
      - npm install -g divshot-cli
      - ember divshot push staging --token $DIVSHOT_TOKEN
