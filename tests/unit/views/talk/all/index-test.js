import { moduleFor, test } from 'ember-qunit';

moduleFor('view:talk/all/index', 'Unit | View | talk/all/index');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
