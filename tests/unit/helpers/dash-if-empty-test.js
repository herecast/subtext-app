import { dashIfEmpty } from '../../../helpers/dash-if-empty';
import { module, test } from 'qunit';

module('Unit | Helper | dash if empty', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dashIfEmpty(42);
    assert.ok(result);
  });
});
